# Reference Redis config for Fardeen. The compose files pass the equivalent as CLI flags
# (so no bind mount is required), but this documents the intended durable setup:
# AOF persistence for the notification-service Redis Stream + no eviction of event data.
appendonly yes
appendfsync everysec
maxmemory-policy noeviction
