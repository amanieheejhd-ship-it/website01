/**
 * @fardeen/config — shared tooling presets (Tailwind, Jest) and design tokens.
 * A leaf package: it depends on nothing inside the workspace (docs/ARCHITECTURE.md §4).
 */
export * from './tokens';
